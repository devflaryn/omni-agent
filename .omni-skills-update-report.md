# omni-agent omnidroid skills update — report

Commit: `5b526dc` "Update omnidroid skills for kiosk failure screen, --place semantics, offset retirement"

## Per-file changes

### skills/omnidroid/SKILL.md
- "The three things to get right" #2: no longer implies a default offset always exists — points at `offset list`.
- "The loop" example: `offset list` comment now flags "can be NONE"; added a fallback `start … --apk build.apk` line and a paragraph explaining a base can ship with zero Roblox baked (arm case, 2026-09-16), `no_offset`, and that a stock Roblox APK boots but can't log in.
- "Roblox versions" per-launch table: default-version row now conditioned on `offset list` showing one; "clean base, no game" row now notes the `--no-offset`/`--apk` bug.
- Errors paragraph: `no_offset` now also covers "the base has none baked at all".
- "Launching an account": added the `session <user> --place` semantics (always re-joins a running instance immediately, `deliver_session(play=True)`; `--play` kept as a no-op) and pointed at it for restarting the game instead of `am force-stop`.
- `debug-info` JSON sample: added `app.kiosk_failure`.
- `app.state` table: `background` row rewritten to say "check `app.kiosk_failure` first" instead of "still launching, re-read after 30s" as the only advice.
- "Signals that are not proof": replaced the stale `session --play` → `played: true` example with `session … --place` → immediate re-join.
- New section **"When the game dies, the kiosk shows a failure screen"**: headline/error-code/detail/countdown behavior, `app.kiosk_failure` shape, "don't relaunch manually while `current: true`", reasons list (`CRASH`, `CRASH_NATIVE`, `ANR`, `LOW_MEMORY`, `SIGNALED`, `FORCE_STOPPED`, `EXIT_SELF`, `BACKGROUND`, `UNAVAILABLE`), `EXIT_SELF`/`SIGNALED` measurements.
- `logcat … --tag OmniKiosk` line: added inline comment showing the `FAILURE attempt=… code=…` line shape.
- Text-only signals list: `OmniKiosk` now called out for the FAILURE relaunch line.
- "Things that will bite you": added the `--no-offset`/`--apk` bug, the `su … kill -9 $(pidof …)` non-substitution quirk, and the arm-offsets-retired fact.
- Did **not** delete the "app.state is the only proof" block — amended it in place (this is the block the live `~/.claude/skills` copy was missing; repo had it, now it also has the `--place` fix).

### skills/omnidroid/reference/omni-cli.md
- Lifecycle `start` paragraph: added a sentence pointing at `app.kiosk_failure`.
- Offsets section: added a paragraph — a base can have zero offsets baked (arm rebake example), don't assume a default, `not_logged_in` for stock APKs.
- `debug-info` JSON sample: replaced the stale/nonexistent offset name `omniexec-2.735.1138-lock2` with a realistic `apk-<sha256[:16]>` cache-offset example, and added `app.kiosk_failure` to the `app` block.
- Added bullets: `offsets_available` can be `[]`, and `app.kiosk_failure` explanation (what `current: true` means, pointer to the new section).
- "Other text signals" line: `OmniKiosk` now described (`FAILURE …` line), and the `su … kill -9 $(pidof …)` non-substitution quirk added.
- New section **"Kiosk failure screen"**: same facts as SKILL.md's, sized for a flag/field reference — headline/code/countdown, the `OmniKiosk` log-line format, reasons list, and "host restarts never trigger it, your own `force-stop` does — use `session --place` instead, and never relaunch manually while `current: true`".
- Accounts and sessions closing paragraph: added the `--place` always-immediate-rejoin fact and the `--play` no-op note.

### skills/omnidroid-input/SKILL.md
- Added a bullet after "Lock Task Mode" in "Things that will bite you": the kiosk is not just a pinner, it self-heals — a screenshot showing a crash headline + `OMNI-<REASON>-<attempt>` + countdown is the new failure screen (no more black screen on death), tapping it relaunches immediately, verify with `state`/`debug-info` rather than trusting the screen change, and pointed at the `omnidroid` skill for full detail.

### vault/Memory/arceus-rebuild-macos-session-2026-09-14.md
- Fixed the one stale line prescribing `session <user> --place N --token-file X --play` as the relaunch recipe: kept `--token-file X` (that part is this project's own SessionProvider-fallback workaround, unrelated to the brief), but noted `--place` now always re-joins immediately on its own and that the trailing `--play` is now a no-op. Rest of the file untouched.

### test/install.test.mjs
- Added pins to the existing "shipped skills document the new omnidroid surface" test: `omni-cli.md` must contain `kiosk_failure` and `--place`; `SKILL.md` must contain `OMNI-`, `--place`, and `kiosk_failure`. No existing pinned substrings were removed.

## Test output

`npm test` (`node --test`): **127 passed, 0 failed, 0 skipped** (full suite, not just install.test.mjs — no regressions elsewhere).

## Installer output

```
node scripts/install.mjs --claude-skills
pi extensions installed: /Users/berat/.pi/agent/extensions/omni-memory.ts, omni-claude.ts, omni-graph.ts
pi config written: /Users/berat/.pi/agent/omni-agent.json
skill installed: /Users/berat/.pi/agent/skills/omnidroid
skill installed: /Users/berat/.pi/agent/skills/omnidroid-input
skill installed: /Users/berat/.claude/skills/omnidroid
skill installed: /Users/berat/.claude/skills/omnidroid-input
Claude Code hook not installed (opt in with --claude-hook).
```

## Diff verification (repo vs installed copies)

All four `diff -r` checks came back **empty** (exact match, no leftover stray files in any installed copy):
- `diff -r skills/omnidroid ~/.claude/skills/omnidroid` → clean
- `diff -r skills/omnidroid-input ~/.claude/skills/omnidroid-input` → clean
- `diff -r skills/omnidroid ~/.pi/agent/skills/omnidroid` → clean
- `diff -r skills/omnidroid-input ~/.pi/agent/skills/omnidroid-input` → clean

This also confirms the original situation described in the brief is resolved: the hand-patched `~/.claude/skills/omnidroid` (which had the `--place` fix but was missing the "app.state is the only proof" block) has been fully replaced by the up-to-date repo copy, which now has both.

## Commit

`5b526dc` — "Update omnidroid skills for kiosk failure screen, --place semantics, offset retirement". Staged only the 5 intended paths (`skills/omnidroid/SKILL.md`, `skills/omnidroid/reference/omni-cli.md`, `skills/omnidroid-input/SKILL.md`, `test/install.test.mjs`, `vault/Memory/arceus-rebuild-macos-session-2026-09-14.md`) via explicit `git add <paths>`; none of the many already-dirty/untracked unrelated files (README.md, server/*, ui/*, vault/Graphs/*, other vault/Memory and vault/Projects notes, docs/superpowers/*, server/hook.mjs, test/hook.test.mjs) were touched or staged.

## Deliberately left stale / out of scope

- `omnidroid` repo itself was not touched (per instructions — read-only source of the facts).
- No VM was booted; all facts came from the brief, which states they were already verified live by the user/another session.
- `HANDOFF.md`'s references to a non-existent `skills/emulator-management/` path (if present in this repo) were **not** touched — out of scope for this task, which was limited to the omnidroid/omnidroid-input skills, the install test, and the one vault note named in the brief. (Not independently re-verified this run; noted per the brief's instruction to call this out rather than silently leave it.)
- The frozen `graphify-out/` knowledge graph was not touched or regenerated — out of scope.
- One judgment call, not a contradiction I was unsure about: the vault note's `--token-file X` in the relaunch recipe was kept as-is (it documents this project's own SessionProvider-fallback workaround, a fact unrelated to the `--play` semantics fix), only the `--play` portion was corrected.

No statements were found that contradicted the brief in a way I couldn't resolve — no items are being flagged as "left stale because contradictory/uncertain."
