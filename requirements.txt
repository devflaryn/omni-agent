# All direct dependencies are pinned to explicit versions for reproducible
# installs. requests is pinned to 2.32.5 (2.32.x line) to move off the
# vulnerable 2.31.0 (GHSA-9wx4-h78v-vm56: .netrc credential leak on redirect).
#
# No full transitive lockfile is committed: on macOS pywebview pulls macOS-only
# pyobjc-* wheels, so a lockfile generated there pins platform-specific
# transitive deps that fail to install on Linux (and vice versa). Without
# pip-tools + a per-platform clean venv, a portable lock is not practical;
# pinning the direct deps here is the reproducibility guarantee that travels.
#
# The reverse-engineering toolchain the agent shells out to (JDK, apktool, jadx,
# radare2, Ghidra, llvm/binutils, ...) is NOT a Python dependency — install it
# with scripts/install_tools.py.
requests==2.32.5
questionary==2.0.1
rich==13.7.0
textual==6.2.1
pillow==11.1.0
pywebview==6.2.1
frida==17.15.4
# selenium drives the headless cookie-login verification (omnidroid accounts.py)
# and the browser-backed local test servers the agent builds for APK redirect
# harnesses. Chrome/Firefox must also be available on the host.
selenium==4.46.0
